#!/bin/bash

set -x

BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

cd "${BUILD_DIR}/static/ns-wifi" && ./waf configure --disable-werror && ./waf build
cd "${BUILD_DIR}/static/ns-lora" && ./waf configure --disable-werror && ./waf build